# Explicit test compose for the historical U20-to-U500 resume contract.
extends: ../../configs/formal_resume_u20_to_u500.yaml

includes:
  - ../../configs/hardware_5x48gb.yaml

rollout:
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.46
  max_num_seqs: 64
